import { AmountMaskingDirective } from './amount-masking.directive';

describe('AmountMaskingDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountMaskingDirective();
    expect(directive).toBeTruthy();
  });
});
